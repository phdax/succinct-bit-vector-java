apply plugin: 'eclipse'
apply plugin: 'java-library'

dependencies {
  compile project(':bitvector')
}

/*
task initSrcDirs {
  doLast {
    sourceSets.all {
      java.srcDirs*.mkdirs()
      resources.srcDirs*.mkdirs()
    }
  }
}
*/

